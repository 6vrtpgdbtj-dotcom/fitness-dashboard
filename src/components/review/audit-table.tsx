import type { AdminWorkspace } from "@/features/admin/types";
const labels: Record<string,string> = { mapping_confirmation: "매핑 확정", member_merge: "회원 병합", trainer_assignment: "담당 배정", trainer_invite: "초대 등록", trainer_activate: "계정 상태 변경", trainer_invitation_claim: "첫 로그인 연결", connection_disconnect: "시트 연결 해제", history_delete: "원본 이력 삭제", record_correct: "필드 수정", record_approve: "검토 승인", record_reject: "집계 제외" };
export function AuditTable({ events }: { events: AdminWorkspace["audits"] }) {
  return <section className="admin-section"><p className="eyebrow">AUDIT TRAIL</p><h2>최근 변경 이력</h2><p className="form-hint">최근 100건 · 변경 주체와 대상 식별자만 표시합니다.</p>{events.length ? <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th scope="col">시간</th><th scope="col">작업</th><th scope="col">대상</th><th scope="col">작업자</th></tr></thead><tbody>{events.map(e => <tr key={e.id}><td>{new Intl.DateTimeFormat("ko-KR",{ timeZone:"Asia/Seoul",dateStyle:"short",timeStyle:"short" }).format(new Date(e.created_at))}</td><th scope="row">{labels[e.action] ?? e.action}</th><td>{e.entity_type}<small>{e.entity_id ?? "—"}</small></td><td>{e.actor_id ?? "자동 동기화"}</td></tr>)}</tbody></table></div> : <p className="admin-empty">아직 변경 이력이 없습니다.</p>}</section>;
}
