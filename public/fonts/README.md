# Local font assets

Place the licensed `PretendardVariable.woff2` and `Archivo-Variable.woff2` files in this folder before production deployment. The application references these self-hosted font paths from `src/app/globals.css` and falls back safely until the licensed files are supplied.
